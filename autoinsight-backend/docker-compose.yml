version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./temp:/app/temp
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped
    # If utilizing a Postgres database later, uncomment and link here
    # depends_on:
    #   - db

  # Optional Postgres DB
  # db:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: autoinsight
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
